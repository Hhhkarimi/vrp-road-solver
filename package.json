{
  "name": "optimasi",
  "private": true,
  "version": "2.2.2",
  "description": "OptiMasir - heterogeneous VRP web app; full map startup fix, robust browser diagnostics, cost/time/distance objectives, imports, KPIs and scenario analysis.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && python3 -m http.server 5173 -d dist"
  },
  "dependencies": {
    "leaflet": "1.9.4",
    "@fontsource-variable/vazirmatn": "5.2.8"
  }
}
